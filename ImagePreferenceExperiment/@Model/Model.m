classdef Model
    %MODEL The model class has information about the screen on which the
    % stimuli are to be rendered. The model class uses this information to
    % transform HDR scenes it receives from the Controller (in XYZ format)
    % data to stimuli specified in RGB settings values for the given display.
    
    properties
    end
    
    methods
        % Constructor
        function obj = Model()
        end
    end
    
end

